apiVersion: v1
kind: Service
metadata:
  name: cua-agent-vnc
  namespace: cua
spec:
  type: NodePort
  selector:
    app: cua-agent
  ports:
    - port: 5901
      targetPort: 5901
      nodePort: 30501